package tools

import (
	"testing"
)

func TestParseRawData(t *testing.T) {
	// data, err := ioutil.ReadFile("./data/testRecord.json")

	// if err != nil {
	// 	fmt.Println("could not read data from file")
	// }

	// parsed := ParseRawData(data)

	// if parsed == nil {
	// 	t.Error("parsed is nil")
	// }

	InsertRawData()
}
